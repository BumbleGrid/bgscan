# bgscan.yaml — generated for dunder-munflin / paper-sales
# generated at 2026-06-23T00:23:58Z
# Recommended: deploy in-cluster with kustomize — see:
#   https://github.com/BumbleGrid/bgscan/blob/main/README.md#deploy-to-kubernetes-kustomize
# Run locally or in-cluster with:
#   bgscan run --config bgscan.yaml

org:               "dunder-munflin"
document:          "paper-sales"
cluster:
  name:            "complex"
  slug:            "complex"
  environment:     "production"
  namespaces:      []

# Required by Step 16's CLI loader (extras tolerated by older builds).
kubeconfig:        ""            # "" → in-cluster / ~/.kube/config
context:           ""            # "" → current-context
namespaces:        []
extractor_version: ""            # bgscan defaults

output:            "push"
endpoint:          "https://bumblegrid.io"
# Push API key (shown once at cluster creation):
api_key:           "bg_sk_TH6gx4l5cZg3w5CPzWTtcEjb2aOgjYr9AqoOB8eDxLo"
